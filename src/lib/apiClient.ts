export {
  authenticatedApiClient as apiClient,
  authenticatedApiClient,
  clearAccessToken,
  getAccessToken,
  hydrateAccessTokenFromStorage,
  setAccessToken,
} from "@/lib/authenticatedApiClient";
