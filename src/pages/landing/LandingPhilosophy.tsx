export function LandingPhilosophy() {
  return (
    <section className="lp-philo" id="philosophy">
      <div className="lp-container" style={{ textAlign: "center" }}>
        <span className="lp-eyebrow">A quiet manifesto</span>
        <h2 className="lp-philo-q" style={{ marginTop: 18 }}>
          A calendar that <em>keeps its promise.</em>
        </h2>
        <p className="lp-lede" style={{ marginTop: 24, marginLeft: "auto", marginRight: "auto", textAlign: "center" }}>
          We build BunnyCal for hosts who care how a meeting begins. The first
          impression of your work is often the email that lands in someone's inbox.
          Make it calm. Make it kind.
        </p>
      </div>
    </section>
  );
}
